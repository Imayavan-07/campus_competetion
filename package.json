{
  "name": "unisync-campus-competition",
  "version": "1.0.0",
  "description": "UniSync Campus Event & Venue Booking Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "campus",
    "events",
    "mysql",
    "react",
    "vite",
    "nodejs",
    "typescript"
  ],
  "author": "",
  "license": "ISC"
}
